{{!-- <header>
        <nav>
            <ul>
                <li class="site-logo">Shoe</li>
                <li>
                    <a href="">
                        <img src="../public/sneakers.png" alt="">
                    </a>
                </li>
                <li class="site-logo">Shelf</li>
            </ul>
        </nav>
    </header> --}}
<main>
    <h1>Login</h1>
    <p class="form-info">Don't have account?
        <a href="/user/register">Register now</a> and fix that!
    </p>
    <form action="/user/login" method="POST">
        <div>
            <input type="email" name="email" placeholder="Email..." value="{{oldInput.email}}">
        </div>

        <div>
            <input type="password" name="password" placeholder="Password..." value="{{oldInput.password}}">
        </div>
        <div>
            <p class="message">{{message}}</p>
            <button>Login</button>
        </div>
    </form>
</main>